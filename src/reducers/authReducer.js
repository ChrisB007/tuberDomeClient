import authAction from "../actions/authAction";

const authPayload = {};

function authReducer(state = authPayload, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default authReducer;
